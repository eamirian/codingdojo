pip install DjangoInstalls the Django web framework for pythonpip listLists all installed packages. At this point I have the following installed.* click (6.7)* Django (1.11.5)* Flask (0.12.2)* itsdangerous (0.24)* Jinja2 (2.9.6)* MarkupSafe (1.0)* pip (9.0.1)* pytz (2017.2)* setuptools (36.4.0)* Werkzeug (0.12.2)* wheel (0.29.0)pip install Django (We know you already ran this one. What information do you see returned in terminal after this command?)It says requirement for this has already been met. Meaning the version matches what we have in the requirements.pip freeze (What's the difference between freeze and list?)pip list – list of installed packagespip freeze – output of installed packages in requirements formatFirst cd into your Desktop directory (cd ~/Desktop), then run this command: pip freeze > requirements.txt. What do you see when you ls? What's inside this file?* click==6.7* Django==1.11.5* Flask==0.12.2* itsdangerous==0.24* Jinja2==2.9.6* MarkupSafe==1.0* pytz==2017.2* Werkzeug==0.12.2pip uninstall DjangoThis uninstalls the Django package.pip show DjangoIf installed, it will show information on Django.pip search Flask. This one might take a moment to execute.Searchs for PyPI packages whose name or summary contains “flask”